# لا ترفعي بيانات حساسة أو ملفات ثقيلة لـ GitHub

# متغيرات البيئة (تحتوي بيانات بوابات الدفع وأسرار أخرى)
backend/.env
frontend/.env.local
# ملف بيئة Docker Compose الحقيقي (المرحلة الثالثة) — منسوخ من
# .env.docker.example، يحمل أسراراً حقيقية. / بأول السطر يحصره بجذر
# المشروع فقط (بلا تأثير على backend/.env المذكور أعلاه، مسار مختلف تماماً).
/.env

# مجلدات التبعيات
node_modules/
backend/node_modules/
frontend/node_modules/

# مخرجات البناء
frontend/build/

# بيانات تشغيلية (تُنشأ تلقائياً، ما تُرفع لـ GitHub)
backend/backups/
backend/data/audit-log.json
backend/data/revoked-tokens.json
backend/data/alerts-log.json
backend/data/monitor-state.json

# ملفات النظام
.DS_Store
Thumbs.db

# سجلات
*.log
npm-debug.log*
