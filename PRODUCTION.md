# 🌐 دليل النشر للإنتاج (خارج جهازك الشخصي)

هذا الدليل يخصّ حالة واحدة فقط: لو قررت نشر النظام على **سيرفر حقيقي** (يصله مستخدمون من أماكن أخرى عبر الإنترنت، وليس فقط جهازك بالمكتب). لو النظام يبقى يعمل محلياً على جهازك فقط (`localhost`)، **لا تحتاج أي شيء بهذا الملف إطلاقاً**.

---

## لماذا HTTPS ضروري بالإنتاج؟

النظام الآن يعتمد بالكامل على كوكي `httpOnly` لتسجيل الدخول. بالتطوير المحلي (`http://localhost`) هذا يعمل تماماً. لكن لو نشرت النظام على الإنترنت بدون HTTPS:
- المتصفح يرفض إرسال الكوكي أصلاً (خيار `secure: true` يفرض هذا تلقائياً بوضع الإنتاج — راجع `NODE_ENV=production` أدناه)، فتسجيل الدخول ينكسر تماماً
- حتى لو تجاوزت هذا، أي بيانات تنتقل بين المتصفح والسيرفر (كلمات مرور، بيانات مرضى) تكون **قابلة للقراءة من أي شخص بنفس الشبكة** (مقهى، فندق، شبكة عامة)

## أبسط طريقة — Caddy (HTTPS تلقائي بالكامل، بدون شهادات يدوية)

[Caddy](https://caddyserver.com) خادم ويب مجاني يتولّى شهادات HTTPS تلقائياً (عبر Let's Encrypt) بدون أي إعداد يدوي — لكن تحتاج **دومين حقيقي** يشير لعنوان IP السيرفر (مثلاً `sihatuna.iq` أو `erp.sihatuna.iq`).

### الخطوات

1. **جهّز دومين** يشير لعنوان IP السيرفر (من أي مزوّد دومينات — GoDaddy، Namecheap، إلخ). أضف سجل DNS من نوع `A` يشير لـ IP السيرفر.

2. **ثبّت Caddy** على السيرفر (Windows Server أو Linux):
   - ويندوز: نزّل الملف التنفيذي من [caddyserver.com/download](https://caddyserver.com/download)
   - لينكس: `sudo apt install caddy` (أو حسب توزيعتك)

3. **أنشئ ملف `Caddyfile`** بجانب مكان تشغيل Caddy:
   ```
   sihatuna.iq {
       # الفرونت إند (يخدم ملفات React المبنية)
       root * C:\ERP\frontend\build
       file_server
       try_files {path} /index.html

       # الباك إند API — أي طلب يبدأ بـ /api يُوجَّه للخادم الحقيقي
       handle /api/* {
           reverse_proxy localhost:8000
       }
       handle /uploads/* {
           reverse_proxy localhost:8000
       }
   }
   ```
   (غيّر `sihatuna.iq` لدومينك الفعلي، و`C:\ERP\...` لمسار مجلد المشروع الفعلي)

4. **ابنِ نسخة الإنتاج من الفرونت إند** (بدل `npm start` المخصّص للتطوير):
   ```cmd
   cd frontend
   npm run build
   ```
   هذا ينتج مجلد `frontend\build` — نفس المجلد المشار له بـ`Caddyfile` أعلاه.

5. **حدّث `backend\.env`:**
   ```
   NODE_ENV=production
   FRONTEND_URL=https://sihatuna.iq
   ```
   `NODE_ENV=production` يفعّل تلقائياً:
   - كوكي تسجيل الدخول تصير `secure: true` (تُرسَل فقط عبر HTTPS)
   - رفض تشغيل الخادم لو `JWT_SECRET` أو `CREDENTIALS_ENCRYPTION_KEY` لا يزالان بالقيمة الافتراضية (حماية إضافية، تأكد أن `setup.bat` ضبط هذا بشكل صحيح)

6. **شغّل الباك إند عادي** (`node server.js` أو `start-backend.bat`) — يبقى يعمل محلياً على المنفذ 8000، وCaddy هو الذي يستقبل حركة الإنترنت الحقيقية على المنفذ 443 (HTTPS القياسي) ويمررها له.

7. **شغّل Caddy:**
   ```cmd
   caddy run
   ```
   بأول تشغيل، Caddy يتصل تلقائياً بـ Let's Encrypt ويحصل على شهادة HTTPS صالحة لدومينك — بدون أي تدخل يدوي منك، وتتجدّد تلقائياً كل فترة بدون أي إجراء إضافي.

الآن `https://sihatuna.iq` يعمل بشهادة HTTPS صالحة وتجديد تلقائي دائم.

---

## بدائل أخرى (لو لديك خبرة تقنية أعلى)

- **nginx + Certbot**: أكثر شهرة، لكن يحتاج إعداد يدوي لتجديد الشهادة (Caddy يقوم به تلقائياً)
- **Cloudflare (وضع Proxy)**: لو دومينك مُدار عبر Cloudflare، تستطيع تفعيل HTTPS من لوحتهم مباشرة بدون تثبيت أي شيء على السيرفر، ويتولّون هم شهادة HTTPS بينهم وبين الزوّار (لا يزال يحتاج HTTPS أو على الأقل نفس الشبكة الداخلية بين Cloudflare وسيرفرك)

---

## 📡 مراقبة السيرفر تلقائياً (تنبيه لو توقّف)

النظام فيه سكربت مراقبة جاهز (`monitor.js` / `monitor.bat`) يفحص صحة السيرفر وقاعدة البيانات، ويرسل تنبيه (سجل محلي دائماً + بريد إلكتروني اختياري) **فقط لو تغيّرت الحالة** — توقّف فجأة، أو رجع طبيعي بعد توقّف.

### التفعيل (خطوة واحدة، مرة واحدة)

1. **افتح "جدولة المهام" بويندوز** (ابحث عن "Task Scheduler")
2. **Create Basic Task** → اسم مثل "SIHATUNA Monitor"
3. **Trigger**: Daily → Recur every 1 day → ثم بعد الإنشاء، افتح خصائص المهمة وغيّر "Repeat task every" لـ **5 minutes** لمدة "Indefinitely" (بتبويب Triggers → Edit → Advanced settings)
4. **Action**: Start a program → اختر ملف `monitor.bat` بمجلد المشروع (`C:\ERP\monitor.bat`)
5. احفظ — بذلك تعمل المراقبة تلقائياً بالخلفية كل 5 دقائق بدون أن تفتح أي شيء يدوياً

### مراجعة التنبيهات
- **سجل دائم**: `backend/data/alerts-log.json` — كل تنبيه صار، بالترتيب الزمني
- **بريد إلكتروني فوري (اختياري)**: عبّ `SMTP_HOST`/`SMTP_USER`/`SMTP_PASS`/`ALERT_EMAIL_TO` بملف `backend/.env` (راجع `.env.example` للتفاصيل والمثال بـ Gmail)

### تجربة سريعة يدوية
```cmd
monitor.bat
```
يقوم بفحص واحد فوري ويطبع النتيجة — تستطيع تشغيله يدوياً بأي وقت للتأكد.

---

## ملاحظة مهمة بخصوص `SameSite=Strict`

راجع التعليق بملف `backend/routes/authRoutes.js` — الكوكي مضبوطة حالياً `SameSite=Strict`، وهذا يفترض الفرونت إند والباك إند على **نفس الدومين** (بالضبط مثل الإعداد أعلاه: كلاهما خلف `sihatuna.iq` بمسارات مختلفة `/` و`/api`). لو خططت لنشر الفرونت إند والباك إند على **نطاقين فرعيين مختلفين تماماً** (مثل `app.sihatuna.iq` و`api.sihatuna.iq` كخادمين منفصلين)، هذا الإعداد يمنع إرسال الكوكي بينهما ويكسر تسجيل الدخول — يجب إرجاعه لـ`'lax'` بهذه الحالة تحديداً.

---

**© 2026 Huda Abduladheem | SIHATUNA IRAQ ERP**
