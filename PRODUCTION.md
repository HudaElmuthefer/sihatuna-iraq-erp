# 🌐 دليل النشر للإنتاج (خارج جهازج الشخصي)

هذا الدليل يخصّ حالة وحدة بس: لو قررتِ تنشرين النظام على **سيرفر حقيقي** (يوصله مستخدمون من أماكن ثانية عبر الإنترنت، مو بس جهازج بالمكتب). لو النظام يبقى يشتغل محلياً على جهازج فقط (`localhost`)، **ما تحتاجين أي شي بهذا الملف إطلاقاً**.

---

## ليش HTTPS ضروري بالإنتاج؟

النظام الحين يعتمد بالكامل على كوكي `httpOnly` لتسجيل الدخول. بالتطوير المحلي (`http://localhost`) هذا يشتغل تمام. لكن لو نشرتِ النظام على الإنترنت بدون HTTPS:
- المتصفح يرفض إرسال الكوكي أصلاً (خيار `secure: true` يفرض هذا تلقائياً بوضع الإنتاج — راجعي `NODE_ENV=production` أدناه)، فتسجيل الدخول ينكسر تماماً
- حتى لو تجاوزتِ هذا، أي بيانات تنتقل بين المتصفح والسيرفر (كلمات مرور، بيانات مرضى) تكون **قابلة للقراءة من أي شخص بنفس الشبكة** (مقهى، فندق، شبكة عامة)

## أبسط طريقة — Caddy (HTTPS تلقائي بالكامل، بدون شهادات يدوية)

[Caddy](https://caddyserver.com) خادم ويب مجاني يتولّى شهادات HTTPS تلقائياً (عبر Let's Encrypt) بدون أي إعداد يدوي — بس تحتاجين **دومين حقيقي** يشير لعنوان IP السيرفر (مثلاً `sihatuna.iq` أو `erp.sihatuna.iq`).

### الخطوات

1. **جهّزي دومين** يشير لعنوان IP السيرفر (من أي مزوّد دومينات — GoDaddy، Namecheap، إلخ). أضيفي سجل DNS من نوع `A` يشير لـ IP السيرفر.

2. **ثبّتي Caddy** على السيرفر (Windows Server أو Linux):
   - ويندوز: نزّلي الملف التنفيذي من [caddyserver.com/download](https://caddyserver.com/download)
   - لينكس: `sudo apt install caddy` (أو حسب توزيعتك)

3. **أنشئي ملف `Caddyfile`** بجانب مكان تشغيل Caddy:
   ```
   sihatuna.iq {
       # الفرونت إند (يخدم ملفات React المبنية)
       root * C:\ERP\frontend\build
       file_server
       try_files {path} /index.html

       # الباك إند API — أي طلب يبدأ بـ /api يُوجَّه للخادم الحقيقي
       handle /api/* {
           reverse_proxy localhost:8000
       }
       handle /uploads/* {
           reverse_proxy localhost:8000
       }
   }
   ```
   (غيّري `sihatuna.iq` لدومينج الفعلي، و`C:\ERP\...` لمسار مجلد المشروع الفعلي)

4. **ابني نسخة الإنتاج من الفرونت إند** (بدل `npm start` المخصّص للتطوير):
   ```cmd
   cd frontend
   npm run build
   ```
   هذا ينتج مجلد `frontend\build` — نفس المجلد المشار له بـ`Caddyfile` أعلاه.

5. **حدّثي `backend\.env`:**
   ```
   NODE_ENV=production
   FRONTEND_URL=https://sihatuna.iq
   ```
   `NODE_ENV=production` يفعّل تلقائياً:
   - كوكي تسجيل الدخول تصير `secure: true` (تُرسَل فقط عبر HTTPS)
   - رفض تشغيل الخادم لو `JWT_SECRET` أو `CREDENTIALS_ENCRYPTION_KEY` لسا بالقيمة الافتراضية (حماية إضافية، تأكدي من `setup.bat` سوّى هذا صح)

6. **شغّلي الباك إند عادي** (`node server.js` أو `start-backend.bat`) — يبقى يشتغل محلياً على المنفذ 8000، وCaddy هو اللي يستقبل حركة الإنترنت الحقيقية على المنفذ 443 (HTTPS القياسي) ويمررها له.

7. **شغّلي Caddy:**
   ```cmd
   caddy run
   ```
   بأول تشغيل، Caddy يتصل تلقائياً بـ Let's Encrypt ويحصل على شهادة HTTPS صالحة لدومينج — بدون أي تدخل يدوي منج، وتتجدّد تلقائياً كل فترة بدون أي إجراء إضافي.

الآن `https://sihatuna.iq` يعمل بشهادة HTTPS صالحة وتجديد تلقائي دائم.

---

## بدائل ثانية (لو عندج خبرة تقنية أعلى)

- **nginx + Certbot**: أكثر شهرة، لكن يحتاج إعداد يدوي لتجديد الشهادة (Caddy يسويه تلقائياً)
- **Cloudflare (وضع Proxy)**: لو دومينج مُدار عبر Cloudflare، تقدرين تفعّلين HTTPS من لوحتهم مباشرة بدون تثبيت أي شي على السيرفر، ويتولّون هم شهادة HTTPS بينهم وبين الزوّار (تحتاجين لسا HTTPS أو على الأقل نفس الشبكة الداخلية بين Cloudflare وسيرفرك)

---

## 📡 مراقبة السيرفر تلقائياً (تنبيه لو توقّف)

النظام فيه سكربت مراقبة جاهز (`monitor.js` / `monitor.bat`) يفحص صحة السيرفر وقاعدة البيانات، ويرسل تنبيه (سجل محلي دائماً + بريد إلكتروني اختياري) **فقط لو تغيّرت الحالة** — توقّف فجأة، أو رجع طبيعي بعد توقّف.

### التفعيل (خطوة وحدة، مرة وحدة)

1. **افتحي "جدولة المهام" بويندوز** (ابحثي عن "Task Scheduler")
2. **Create Basic Task** → اسم مثل "SIHATUNA Monitor"
3. **Trigger**: Daily → Recur every 1 day → ثم بعد الإنشاء، افتحي خصائص المهمة وغيّري "Repeat task every" لـ **5 minutes** لمدة "Indefinitely" (بتبويب Triggers → Edit → Advanced settings)
4. **Action**: Start a program → اختاري ملف `monitor.bat` بمجلد المشروع (`C:\ERP\monitor.bat`)
5. احفظي — خلاص، المراقبة تشتغل تلقائياً بالخلفية كل 5 دقائق بدون ما تفتحين أي شي يدوياً

### مراجعة التنبيهات
- **سجل دائم**: `backend/data/alerts-log.json` — كل تنبيه صار، بالترتيب الزمني
- **بريد إلكتروني فوري (اختياري)**: عبّي `SMTP_HOST`/`SMTP_USER`/`SMTP_PASS`/`ALERT_EMAIL_TO` بملف `backend/.env` (راجعي `.env.example` للتفاصيل والمثال بـ Gmail)

### تجربة سريعة يدوية
```cmd
monitor.bat
```
يسوي فحص واحد فوري ويطبع النتيجة — تقدرين تشغّلينه يدوياً بأي وقت للتأكد.

---

## ملاحظة مهمة بخصوص `SameSite=Strict`

راجعي التعليق بملف `backend/routes/authRoutes.js` — الكوكي مضبوطة حالياً `SameSite=Strict`، وهذا يفترض الفرونت إند والباك إند على **نفس الدومين** (بالضبط متل الإعداد أعلاه: كلاهما خلف `sihatuna.iq` بمسارات مختلفة `/` و`/api`). لو خططتِ لنشر الفرونت إند والباك إند على **نطاقين فرعيين مختلفين تماماً** (مثل `app.sihatuna.iq` و`api.sihatuna.iq` كخادمين منفصلين)، هذا الإعداد يمنع إرسال الكوكي بينهما ويكسر تسجيل الدخول — لازم ترجعينه لـ`'lax'` بهذي الحالة تحديداً.

---

**© 2026 Huda Abduladheem | SIHATUNA IRAQ ERP**
